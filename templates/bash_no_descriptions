_COMMAND() {
  COMPREPLY=()
  local cur
  cur=$(_get_cword)
  IFS='' read -r -d '' completions <<'EOF'
ARGUMENTS
EOF

  COMPREPLY=( $( compgen -W "$completions" -- "$cur" ) )

  return 0
}
complete -F _COMMAND COMMAND
